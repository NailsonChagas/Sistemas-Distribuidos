#ifndef MERGE_SORT_PARALLEL_H
#define MERGE_SORT_PARALLEL_H

/**
 * @brief Ordena um array de inteiros usando Merge Sort Paralelo (pthreads).
 * * @param array Ponteiro para o array original.
 * @param size  Tamanho do array.
 */
void merge_sort_parallel(int *array, int size);

#endif